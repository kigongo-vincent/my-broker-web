const usePosts = () => {
  // the function should be a hook for getting posts data, under the hood it should use utility function of GET, and get under the hood should user react query and propagate the data back with state info,
};
